import { Module } from '@nestjs/common';

/**
 * Worker composition root.
 *
 * Milestone 5 adds ConfigModule, DatabaseModule, the SQS consumer, the S3
 * upload adapter, the CSV generator, and CloudWatch metrics here.
 */
@Module({})
export class AppModule {}
